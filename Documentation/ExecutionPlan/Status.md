# Execution status

Phase 00 candidate: official OpenAPI 4.4.1 pinned, 131 operation contracts inventoried, domain conditions documented, 22 contract/documentation checks passing. Full-schema reproducibility check passed. No live Apple calls, runtime MCP, or publication yet. Phase 01 and later remain pending.

The attached combined implementation plan is preserved at the root. Canonical phase documents are split under this directory. Missing Architecture, API-Capabilities, Sources and Headroom procedure documents were created from the supplied product/format/plan contracts with fresh official API evidence. The original numbered S/R bibliography was not supplied; Sources documents that limitation.

CI and nightly match the sibling workflow layout and macOS runner, with Node 24 and contract tests for this pre-package phase. Phase 01 adds npm clean install/build and Phase 12 adds guarded publishing. README badges use the sibling design with this repository's URLs; NPM remains labelled planned until publication.

Initial PR review required a correction to availability semantics: generic territory writes are now explicitly disabled/conditional with a manual fallback; pre-order writes are outside v1. Local documentation links are checked automatically.

Phase 00 merged via PR #1 at 9fb8183 after exact-head Relay approval and green CI. Phase 01 candidate implements strict ESM package, lazy team-key JWT, safe bounded transport, offline stdio capabilities and package validation. 40 checks plus clean production tarball install/handshake passed locally; no live Apple calls. Publication workflow remains Phase 12.

Phase 01 merged through PR #2 after exact-head Relay approval and green Node24 CI. Phase 02 candidate adds strict schemas, secure file reading, scoped offline validation and process-keyed secret comparison. Generated schema drift checks, 54 tests and clean package validation pass; decoded image validation and creation/readiness checks remain later phases.

Phase 02 merged PR #3 after exact-head Relay approval and green CI. Phase 03 candidate adds GET-only discovery, verified identity/version selection, redacted state and fresh-directory export. 64 tests and package validation pass. Headroom live reads confirmed only en-US, no screenshots, version1.0 in PREPARE_FOR_SUBMISSION and existing AFTER_APPROVAL release behavior. Eight-file baseline exported privately; no Apple writes. See Acceptance-Phase-03.md.

Phase 03 merged through PR #4 after exact-head Relay approval and green CI (69 tests). Phase 04 candidate implements the shared immutable plan engine and status/apply tools with mock adapters. No remote domain writes are enabled yet. Safety tests cover changed bindings, approved subsets, partial failure, unknown outcomes, durable intent, redaction and account serialization.

Phase 04 merged through PR #5 at 9db5985 after exact-head Relay approval and green CI. Phase 05 candidate adds bounded provisioning and static Xcode inspection, with a scope-qualified planner-ID improvement from the review. Headroom UNIVERSAL bundle discovery and a zero-operation plan passed with zero Apple write requests. Signing mutation coverage remains mocked, not live.

Phase 05 merged through PR #6 at b4410e8 after exact-head Relay approval and green CI (107 tests). Phase 06 candidate adds independent metadata families and explicit shared version/category/review operations. The stale API-Capabilities table noted by review is synchronized. Live English-only metadata planning reproduced the exported baseline as a no-op using five GET endpoints and zero writes. Four caller-authored translations validate locally; mutation acceptance follows.

Phase 06 live metadata gate passed at 2026-09-06 21:54 UTC: four AppInfo POSTs and four version-localization PATCHes, all selected fields verified, original English fingerprint and shared version fields unchanged, and a second zero-operation plan. Apple-initialized empty companions are explicitly handled after an initial safe stop without replay. See Acceptance-Phase-06.md. All 122 tests and a clean 67-file package check passed.

Phase 06 review correction binds the final resolved review payload to approved hashes immediately before dispatch, with three deterministic late-change/zero-write tests. Historical REPLACED_WITH_NEW_VERSION entries no longer block eligible version creation/update notes. The complete pipeline now passes 126 tests and the same 67-file package check.

Phase 06 merged through PR #7 at 307fc29 after exact-head Relay approval and green Node24 CI. Phase 07 candidate adds packaged native decoded image validation, a separate pinned-public-DNS HTTPS asset client and a journaled reserve/transfer/commit/process lifecycle. All 142 tests and clean installation of the 76-file package pass, including the installed Swift helper. Transfer coverage is synthetic; no live screenshots were uploaded and high-level screenshot plans remain Phase 08.

Phase 07 merged through PR #8 at de77a5f after exact-head Relay approval with no findings and green Node24 CI. Phase 08 candidate adds approved locale/set reconciliation, byte-equivalence no-ops, complete relationship ordering, explicit replacement/capacity dependencies and interrupted-upload protection. The full pipeline passes 157 tests and an 82-file package smoke. Runtime capabilities now derive from the packaged capability resource. Headroom live screenshot acceptance remains pending genuine owner-supplied originals; no placeholders have been uploaded.

Phase 08 merged through PR #9 at 17688e4 after exact-head Relay approval with no findings and green Node24 CI. Phase 09 candidate adds exact-decimal base-price planning against app/territory-scoped catalog points, refuses complex schedules, and compares complete territory availability without invoking pre-order-specific mutation routes. A live Headroom read used 18 GETs and zero writes: free in USA, 175 configured territories, future territories enabled, with Apple simultaneously reporting CANNOT_SELL and AVAILABLE_FOR_SALE_UNRELEASED_APP. The full response remains private and excluded from the package.
