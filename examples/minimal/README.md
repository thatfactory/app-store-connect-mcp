# Minimal format example

This synthetic root passes offline structural validation. Omitted URLs, prose, review, commerce and screenshot fields are unmanaged; this is not a complete new listing or evidence of release readiness. Replace identity and author explicit desired fields before planning against a real account. Examples are excluded from the npm tarball.
